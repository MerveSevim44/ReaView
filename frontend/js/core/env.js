/**
 * Environment Configuration
 *
 * This file acts as a .env for browser-based projects.
 * Browsers cannot natively read .env files, so this JS file mirrors it.
 *
 * ⚙️  To change the backend URL:
 *   1. Edit API_BASE_URL below
 *   2. Also update frontend/.env (kept in sync for reference / build tools)
 */

// Mirrors: frontend/.env → API_BASE_URL
export const API_BASE_URL = "https://reaview.vercel.app";

