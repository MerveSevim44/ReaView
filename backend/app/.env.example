# Example .env for ReaView backend
# Use a proper Postgres URL in production, for example:
# DATABASE_URL=postgresql+psycopg2://user:password@hostname:5432/dbname?sslmode=require

# For local development with PostgreSQL replace the line above with your connection string.

# If you want to use SQLite for local testing you can set:
# DATABASE_URL=sqlite:///./dev.db
